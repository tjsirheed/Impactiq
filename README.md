
  # ImpactIQ

  # This was generated using Figma AI
  
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  I joined a team of 3 for an hackathon and we needed to present what our proposed mobile app dashboard will look like so I decided to test out my prompting skills on Figma AI.
